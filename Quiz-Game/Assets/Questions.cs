using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Questions : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//Load XML file with listed questions and answers
	}
	
	// Update is called once per frame
	void Update () {
		//Create a percentage correct of the questions and update as the user completes the tests
	}
}
